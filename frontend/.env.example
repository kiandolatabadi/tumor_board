# Copy to .env.local for local overrides, or set in your host's build env.
# Dev leaves this empty and uses the Vite proxy (/api -> localhost:8000).
# For a deployed frontend, set this to your hosted backend's URL:
# VITE_API_BASE=https://your-backend.onrender.com
VITE_API_BASE=
